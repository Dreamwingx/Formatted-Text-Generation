# Base Project With Logging

一个简单的Python项目，用于______。

## 功能特性

- 第一个功能
- 第二个功能

## 安装依赖

```bash
pip install -r requirements.txt
```

## github维护
```bash
git config --global http.sslVerify false
git push -u origin main
git config --global http.sslVerify true
```